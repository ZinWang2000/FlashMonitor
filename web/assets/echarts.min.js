/*
 * ECharts placeholder - for offline use, download the actual ECharts library:
 *
 *   https://cdn.jsdelivr.net/npm/echarts@5/dist/echarts.min.js
 *
 * Save it here as web/assets/echarts.min.js and rebuild the executable.
 *
 * The HTML report uses the CDN URL by default (defined in report/html_writer.go).
 * To use this local version, update the SetEChartsScript() call in main.go.
 */
